# Task list for adding ship model map and loader

1. Create `src/config/shipModelMap.ts` - add type and map for 5 ship classes (not-started)
2. Create `src/core/shipModelLoader.ts` - loader adapter that uses existing glTF stub and registers assets in `GameState.assetPool` (not-started)
3. Typecheck and run unit tests for quick validation (not-started)

Update: use repo conventions and GameState.assetPool API.
