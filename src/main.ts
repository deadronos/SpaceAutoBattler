// TypeScript entrypoint wrapper — delegates to existing JS module for now.
// This allows the build to treat the app as TypeScript while we incrementally port internals.
export * from './main.js';
