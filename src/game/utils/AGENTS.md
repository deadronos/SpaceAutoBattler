```markdown
# Agents Guide: src/game/utils

- Purpose: Small game-specific helpers shared by systems (math helpers, clampers, spawn utilities).
- Purity: Prefer pure functions that accept and return values rather than mutating global state.
- Tests: Unit-test all utility functions with deterministic inputs; favour edge-case coverage.

```
