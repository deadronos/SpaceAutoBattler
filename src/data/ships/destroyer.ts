import { Vector3 } from 'three';
import type { ShipStats } from '../../types/index.js';
import { HULL_DAMAGE_TYPES, HULL_ARMOR_VALUES } from '../../config/progression.js';
import {
  createHeavyTurret,
  createLaserTurret,
  createTorpedoTurret,
  createMissileTurret,
  createBeamTurret,
} from './turret-factory.js';

export const DESTROYER_STATS: ShipStats = {
  hull: 'destroyer',
  maxHp: 250,
  maxShield: 433,
  shieldRegen: 24.0,
  damage: 30,
  fireRate: 1.8,
  projectileSpeed: 67,
  range: 700,
  speed: 10,
  scale: 1,
  bulletType: 'bullet:heavy',
  damageType: HULL_DAMAGE_TYPES.destroyer ?? 'kinetic',
  armor: HULL_ARMOR_VALUES.destroyer ?? 10,
  motion: {
    mass: 4.0,
    maxSpeed: 10,
    maxReverseSpeed: 2.5,
    linearAcceleration: 10,
    linearDamping: 1.5,
    maxTurnRate: Math.PI * 0.6,
    angularAcceleration: Math.PI * 1.8,
    angularDamping: 4.0,
    turnKp: 3.6,
    turnKd: 0.95,
    angularSettlingRate: 0.008,
    angularSettleToleranceDeg: 4.5,
    maxLateralAcceleration: 4,
    visualBankFactor: 10,
    maxBankDeg: 22,
    visual: {
      enabled: true,
      position: { k: 10.0 },
      rotation: { k: 22.0 },
      bank: { k: 14.0, maxDeg: 22, useCriticallyDamped: true },
      teleportDistance: 42,
      bob: { enabled: false, baseAmp: 0.06, freq: 0.9, speedScale: 1.0, maxAmp: 0.25 },
    },
  },
  sensor: {
    detectionRange: 780,
    trackingRange: 940,
    coneAngle: Math.PI * 0.6,
    falloff: 0.7,
  },
  stealth: 0,
  sensorSignature: 1.5,
  turrets: [
    createHeavyTurret(new Vector3(1.6, 0.3, -0.2)),
    createHeavyTurret(new Vector3(-1.6, 0.3, -0.2)),
    createLaserTurret(new Vector3(0.9, 0.3, 0.6), {
      damage: 10,
      fireRate: 1.6,
      range: 600,
      minYaw: -Math.PI * 0.7,
      maxYaw: Math.PI * 0.7,
      minPitch: -Math.PI * 0.25,
      maxPitch: Math.PI * 0.45,
    }),
    createLaserTurret(new Vector3(0.9, 0.3, 0.6), {
      damage: 10,
      fireRate: 1.6,
      range: 600,
      minYaw: -Math.PI * 0.7,
      maxYaw: Math.PI * 0.7,
      minPitch: -Math.PI * 0.25,
      maxPitch: Math.PI * 0.45,
    }),
    createLaserTurret(new Vector3(-0.9, 0.3, 0.6), {
      damage: 10,
      fireRate: 2.0,
      range: 600,
      minYaw: -Math.PI * 0.7,
      maxYaw: Math.PI * 0.7,
      minPitch: -Math.PI * 0.25,
      maxPitch: Math.PI * 0.45,
    }),
    createLaserTurret(new Vector3(-0.9, 0.3, 0.6), {
      damage: 10,
      fireRate: 2.0,
      range: 600,
      minYaw: -Math.PI * 0.7,
      maxYaw: Math.PI * 0.7,
      minPitch: -Math.PI * 0.25,
      maxPitch: Math.PI * 0.45,
    }),
    createLaserTurret(new Vector3(2.1, 0.45, 0.85), {
      damage: 10,
      fireRate: 2.0,
      projectileSpeed: 250,
      range: 600,
      minYaw: -Math.PI * 0.8,
      maxYaw: Math.PI * 0.8,
      minPitch: -Math.PI * 0.35,
      maxPitch: Math.PI * 0.55,
    }),
    createLaserTurret(new Vector3(-2.1, 0.45, 0.85), {
      damage: 10,
      fireRate: 2.0,
      projectileSpeed: 250,
      range: 600,
      minYaw: -Math.PI * 0.8,
      maxYaw: Math.PI * 0.8,
      minPitch: -Math.PI * 0.35,
      maxPitch: Math.PI * 0.55,
    }),
    createTorpedoTurret(new Vector3(1.8, 0.25, -1.4)),
    createTorpedoTurret(new Vector3(-1.8, 0.25, -1.4)),
    createMissileTurret(new Vector3(1.2, 0.35, -1.8), {
      damage: 22,
      projectileSpeed: 96,
      range: 660,
      minYaw: -Math.PI * 0.4,
      maxYaw: Math.PI * 0.4,
    }),
    createMissileTurret(new Vector3(-1.2, 0.35, -1.8), {
      damage: 22,
      projectileSpeed: 96,
      range: 660,
      minYaw: -Math.PI * 0.4,
      maxYaw: Math.PI * 0.4,
    }),
    createBeamTurret(new Vector3(0, 0.4, 1.6), {
      damage: 18,
      fireRate: 2.4,
      range: 680,
    }),
  ],
};
