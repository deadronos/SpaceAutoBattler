# Progress — SpaceAutoBattler

What works
- Core deterministic simulation and renderer build pipelines are operational.
- Standalone build scripts and smoke tests exist under `tmp/` and `scripts/`.
- Memory bank with per-file summaries exists under `memory/`.

What's left
- Expand developer docs (llms.txt -> docs/_index.md).
- Add CI link checks and automated memory regeneration workflows.

Known issues
- Some dev scripts rely on Node features that vary by version; we recommend Node 18+.

Generated/updated: 2025-09-02 by Serena agent.