# src/config/projectiles.ts

Path: src/config/projectiles.ts
Last-Reviewed: 2025-09-21

Purpose: Projectile configuration values used by both simulation and renderer when necessary (speed, lifetime, damage categories).

Key exports/symbols:
- PROJECTILE_CONFIG

Notes:
- Keep values deterministic and use config-driven constants rather than magic numbers.
