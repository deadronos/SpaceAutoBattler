# src/main.tsx

Path: src/main.tsx
Last-Reviewed: 2025-09-21

Purpose: Application bootstrap. Applies loader patches and mounts React app. Handles service worker or other entrypoint-level concerns.

Key exports/symbols:
- n/a (entrypoint script)

Notes:
- Coordinates renderer initialization and global asset patches in `src/utils/patchGltfLoader.ts`.
