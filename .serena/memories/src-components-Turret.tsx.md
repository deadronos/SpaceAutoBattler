# src/components/Turret.tsx

Path: src/components/Turret.tsx
Last-Reviewed: 2025-09-21

Purpose: Visual turret subcomponent used by `Ship` to render rotating turrets.

Key exports/symbols:
- Turret component

Notes:
- Visual only; turret targeting and firing logic lives in `src/game/turretRegistry.ts` and turret systems.
