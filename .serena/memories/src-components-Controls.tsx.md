# src/components/Controls.tsx

Path: src/components/Controls.tsx
Last-Reviewed: 2025-09-21

Purpose: UI controls for spawning teams, pausing, stepping simulation, and toggles for visuals.

Key exports/symbols:

- Controls component

Notes:

- Interacts with `src/game/uiStore.ts` for user-driven state.
