# update_bullets_api (HISTORICAL)

Last-Reviewed: 2025-09-21

Status: MARKED HISTORICAL

Notes:

- Projectile advancement and hit-resolution live in `src/game/systems.ts` (`advanceProjectiles` / `resolveProjectiles`). Consult that file for current tick semantics and collision resolution.
