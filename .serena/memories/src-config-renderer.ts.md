# src/config/renderer.ts

Path: src/config/renderer.ts
Last-Reviewed: 2025-09-21

Purpose: Visual configuration such as shield material parameters, postprocess toggles, and LOD settings.

Key exports/symbols:
- RENDERER_CONFIG

Notes:
- Renderer-only configuration; keep separate from simulation `src/game/config.ts`.
