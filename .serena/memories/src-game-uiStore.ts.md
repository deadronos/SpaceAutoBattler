# src/game/uiStore.ts

Path: src/game/uiStore.ts
Last-Reviewed: 2025-09-21

Purpose: A small Zustand store for UI-driven state like paused, speed multiplier, and selections.

Key exports/symbols:

- useUiStore

Notes:

- UI store is separate from `GameState` and drives user interactions only.
