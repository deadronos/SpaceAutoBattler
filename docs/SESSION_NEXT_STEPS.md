# Debugging missing bullets — session summary & next steps

Date: 2025-09-06

Summary
- Investigated missing bullet visuals (user reported "no bullets are spawned").
- Instrumented both simulation and renderer to gather evidence.
- Added an opt-in guarded debug accessor to expose a safe GameState snapshot at runtime via `?debugState=1` (in `threeRenderer.ts`).
- Added a Playwright smoke test (`test/playwright/bullet-visual.spec.ts`) which opens the app with `?debugState=1`, clicks Start/Speed, captures console and screenshot, calls `__GAME_STATE__.getSnapshot()` and writes `test-output/state-snapshot.json`.
- Enhanced that Playwright test to scan the snapshot for non-finite coordinates and write `test-output/state-snapshot-report.json`, failing the test if any non-finite coords found.
- Added defensive guards and logging in renderer transform codepaths to avoid Three.js errors when non-finite transforms occur and to log offending entity ids:
  - `src/renderer/synchronizer.ts` — detect non-finite ship/bullet transforms and log `[SYNC_ERROR][updateTransforms]`.
  - `src/renderer/shipInstancer.ts` — log `[INSTANCER_ERROR][ShipInstancer]` when updateTransform receives non-finite inputs.
  - `src/renderer/bulletInstancer.ts` — log `[INSTANCER_ERROR][BulletInstancer]` when bullet pos is non-finite.

Artifacts produced
- `test-output/console.log` — Playwright-captured console output
- `test-output/bullet-visual.png` — screenshot from Playwright run
- `test-output/state-snapshot.json` — snapshot returned by `__GAME_STATE__.getSnapshot()`
- `test-output/state-snapshot-report.json` — snapshot scan report (offenders list)

What we observed
- The snapshot in sampled frames showed finite ship positions and an empty bullets array.
- Playwright-run console earlier contained HB_TRACE lines with `pos= ni` (three.js reporting non-finite components) in previous runs, motivating defensive logging.
- Current instrumented run did not detect non-finite positions at the sampled frame.

Next steps (prioritized)
1. Increase sampling to catch transient NaNs (highest priority)
   - Update `test/playwright/bullet-visual.spec.ts` to poll `__GAME_STATE__.getSnapshot()` every ~100ms for 3–5s and fail/report on first non-finite find.
   - Acceptance: test fails if any non-finite coords observed; artifacts saved under `test-output/` with timestamped snapshots.
   - Command to run:

```powershell
npx playwright test test/playwright/bullet-visual.spec.ts --config=playwright.config.cjs --project=chromium
```

2. If NaNs found, instrument simulation math (turret aiming, lead calculations, `ProjectileSystem.fire`)
   - Add localized logs that record entity id, input vectors, intermediate floats, and RNG seed for deterministic reproduction.
   - Acceptance: logs show entity id and offending intermediate value within the same simulation tick as the snapshot.

3. Correlate logs and snapshots
   - When a non-finite is found, collect the matching `state-snapshot.json`, console logs, and any new simulation logs; produce a minimal reproducer test harness (unit test) that runs the simulation step(s) and asserts on the upstream value.

4. Fix root cause and add tests
   - Implement a minimal fix (guard, clamp, or correct math) and add unit tests reproducing the failing case.
   - Acceptance: `npm run typecheck && npm test` passes; Playwright snapshot-scan shows no NaNs over N runs.

5. PR & cleanup
   - Prepare PR describing the telemetry, the root-cause fix, tests added, and instructions to reproduce.

Notes & tips
- The guarded accessor `__GAME_STATE__` is opt-in (only enabled when URL has `?debugState=1`) so it is safe for production builds if kept behind the flag.
- Defensive logging uses `logger.error` with structured objects to make it easy to grep/parse.
- If logs are noisy, add a small rate-limiter or dedupe by entity id to avoid flooding the console.

Contact & follow-up
- If you want, I can implement step (1) now (looped Playwright snapshot check) and run it to try to catch transient NaNs. Say: "run looped snapshot check" to proceed.


---
Generated by an automated debugging session assistant; commit or edit as you prefer.
