# Source files in `src/` with more than 500 lines (code-only)

This file was regenerated to exclude binary asset files (e.g., `.glb`, `.fbx`, `.png`, `.jpg`, `.blend`). Line counts were measured on the host machine.

Code files found (over 500 lines):

- 1245 lines — `src/game/systems.ts`
- 532 lines — `src/game/ships.ts`

Notes:

- The previous version included binary assets because they exceed 500 lines when read as text. This version intentionally lists only source-code files.
- If you'd like a list including other text-like assets (e.g., large JSON, shaders), tell me which extensions to include and I can regenerate the file.
