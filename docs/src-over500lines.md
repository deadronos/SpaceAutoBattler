# Source files in `src/` with more than 500 lines (code-only)

This file was regenerated by scanning all TypeScript and TSX files in `src/` to
identify source files that exceed 500 lines of code. Binary assets and
non-source files (GLB, PNG, JPG, etc.) are excluded from the scan.

Code files found (over 500 lines):

- 1245 lines — `src/game/systems.ts`
- 532 lines — `src/game/ships.ts`

Notes:

- The scan considered files with the extensions `.ts` and `.tsx` under
  `src/` only.
- Line counts were measured on the host machine at the time of generation
  (2025-09-27).
- If you would like the report regenerated to include other extensions
  (for example, large JSON or shader files), request the extensions and
  the file will be re-scanned.
